import React from 'react';

function App() {
  return (
    <div className="min-h-screen py-16 px-6 md:px-12 flex flex-col items-center">
      <div className="w-full max-w-2xl">
        <header className="mb-12">
          <h1 className="text-2xl font-semibold">Ready to build</h1>
          <p className="text-secondary mt-2">Tech stack (React + Tailwind) is successfully configured.</p>
        </header>

        <main className="space-y-12">
          {/* Sections will go here */}
        </main>
      </div>
    </div>
  );
}

export default App;
